package com.hartwig.hmftools.isofox;

public enum TaskType
{
    FRAGMENT_LENGTHS,
    TRANSCRIPT_COUNTS,
    GENERATE_GC_COUNTS,
    GENERATE_EXPECTED_COUNTS,
    APPLY_GC_ADJUSTMENT,
    BAM_READ_COUNTER;
}
