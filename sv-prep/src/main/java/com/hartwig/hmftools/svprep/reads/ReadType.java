package com.hartwig.hmftools.svprep.reads;

public enum ReadType
{
    NO_SUPPORT,
    CANDIDATE_SUPPORT,
    SUPPORT,
    EXACT_SUPPORT,
    JUNCTION,
    EXPECTED,
    RECOVERED,
    BLACKLIST,
    UNMATCHED;
}
